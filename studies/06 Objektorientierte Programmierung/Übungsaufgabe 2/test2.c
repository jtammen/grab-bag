// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
// Beispiel: Monatsname
// --------------------

char * monatName( int monatNr);     /* Prototyp */

void main( )
{
    int c = 0;

    for( ; ; )
    {
        printf( "\nZahl ? (<=0 Ende) :" );
        scanf ( "%d", &c);
        if (c <= 0) return;
        printf( "\n%d  %s\n", c, monatName( c) );
    }
}
